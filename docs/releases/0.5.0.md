# 0.5.0 发布记录

日期：2026-09-05。内容为本地视觉实验台及发布前的存储冲突、姓名编辑修复。

| 层级 | 证据 | 结果 |
| --- | --- | --- |
| 本地 | `21b3c43fd52d6a39e6458b562d373de919e23c79`；`npm run check`、`npm run test:e2e:smoke`、`git diff --check` | 554 项单元测试、类型检查、Lint、生产构建、21 项浏览器流程通过 |
| CI | [Actions 33967146215](https://github.com/LongXL6/fpvhelper/actions/runs/33967146215) | 失败任务 `steps: []`，未运行测试；用户明确要求不等待 CI |
| PR / 合并 | [PR #6](https://github.com/LongXL6/fpvhelper/pull/6)；`5bc6b63bf1e6d10d886aa0210c89d04a3956449b` | 已合并 |
| Preview | `dpl_FNstvHNeaUayqjAStSiwWSvTQUGf` | Vercel `READY`；未取得独立 Preview HTTP 验证，不将其记为已验证页面 |
| 生产部署 | `dpl_DjVKDtfCcCSs5mXMER9MZEq7XS6P` | Vercel Git 集成自动发布，`READY`、`target: production`，生产别名指向此构建 |
| 生产 HTTP | [视觉实验台](https://race.fpvsuperapp.com/vision-lab)、[版本](https://race.fpvsuperapp.com/version.json) | 页面 HTTP 200 且含实验台内容；版本 `0.5.0+5bc6b63`，`environment: production` |
| 版本标签 | annotated tag `v0.5.0` 指向 merge commit `5bc6b63` | 生产核验后已推送 |
| 真机与模型精度 | 合成素材真实 Worker/WASM 实验见[实验台文档](../vision-lab.md) | 未证明真实 FPV 穿越准确率、物理计时精度或真机持续采集 |

本机系统 DNS 当时不稳定。生产 HTTP 使用 Google HTTPS DNS 实际返回的站点地址作进程内解析，并保留正常 HTTPS 主机名验证；未修改系统 DNS。GitHub 与 Vercel 操作使用临时本地 HTTPS CONNECT 转发。生产页面、版本、Provider 别名和本地测试分别核验，不相互替代。
